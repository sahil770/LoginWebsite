import { FormstyleDirective } from './formstyle.directive';

describe('FormstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new FormstyleDirective();
    expect(directive).toBeTruthy();
  });
});
